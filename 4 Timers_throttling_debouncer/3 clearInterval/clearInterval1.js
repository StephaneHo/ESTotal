// invoke it in the console window without open live server (use normal console window of google)
setInterval(() => console.log("et voila !!"), 1000);

// invoke several times and remark it returns serveral IDs

// invoke clearInterval and show that it stops the setInterval
